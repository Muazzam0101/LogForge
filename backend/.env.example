# LogForge Backend Environment Configuration
PROJECT_NAME="LogForge ULPF Core Engine"
VERSION="0.1.0"
DEBUG=False

# MySQL Connection URL (Default: local MySQL 8.0 instance)
DATABASE_URL=mysql+pymysql://root:admin@localhost:3306/logforge

# Alternative: PostgreSQL Connection URL
# DATABASE_URL=postgresql+psycopg://logforge:logforge@localhost:5432/logforge

# Alternative: SQLite for isolated zero-dependency testing
# DATABASE_URL=sqlite:///./logforge.db

# Connection Pool Settings
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_ECHO=False

# Blockchain Integrity Anchoring (Optional / Modular)
# Set to false for pure offline / air-gapped deployments without blockchain node
BLOCKCHAIN_ENABLED=false
BLOCKCHAIN_RPC_URL=http://127.0.0.1:8545
BLOCKCHAIN_CONTRACT_ADDRESS=
BLOCKCHAIN_PRIVATE_KEY=
BLOCKCHAIN_NETWORK=local-evm

# OpenSearch Distributed Search & Analytics (Optional / Scalability Layer)
# When set to false, LogForge operates seamlessly using MySQL-backed search alone
OPENSEARCH_ENABLED=false
OPENSEARCH_URL=http://localhost:9200
OPENSEARCH_INDEX=logforge-events
OPENSEARCH_BULK_SIZE=500
OPENSEARCH_TIMEOUT=10
OPENSEARCH_MAX_RETRIES=3
OPENSEARCH_AUTH_USER=
OPENSEARCH_AUTH_PASSWORD=
OPENSEARCH_USE_SSL=false
OPENSEARCH_VERIFY_CERTS=false
OPENSEARCH_REFRESH_INTERVAL=1s

# Apache Kafka Distributed Streaming
KAFKA_ENABLED=false
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_LOG_TOPIC=logforge.raw-events
KAFKA_DLQ_TOPIC=logforge.dead-letter
KAFKA_CONSUMER_GROUP=logforge-ulpf-workers
KAFKA_BATCH_SIZE=100
KAFKA_COMPRESSION=snappy
KAFKA_NUM_PARTITIONS=3
KAFKA_REPLICATION_FACTOR=1

# ULPF Worker Scalability & Batch Processing Tuning
ULPF_BATCH_SIZE=100
ULPF_WORKER_CONCURRENCY=1
PERFORMANCE_METRICS_ENABLED=true
